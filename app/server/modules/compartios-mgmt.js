// compartios Manager
var Compartio = require('../model/compartio');
/** var mongoose = require('mongoose');
var configDB = require('./config/database.js');

var db = mongoose.connect(configDB.url, function(err) {
    if (err) throw err;
}); **/

exports.findAllCompartios = function (req, res) {

	Compario.find
}